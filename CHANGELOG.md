<div dir="rtl">

# سجل التغييرات

جميع التغييرات المهمّة في هذا المشروع تُسجَّل هنا.

يتبع هذا الملف صيغة [Keep a Changelog](https://keepachangelog.com/ar/1.1.0/)،
ويتبع المشروع [الترقيم الدلالي](https://semver.org/lang/ar/).

</div>

## [1.1.0] — 2026-08-30

<div dir="rtl">

### أُضيف (Added)

- **`shamela_search_coverage`: أين تقع النتائج، لا نصوصها.** أداة تعدّ المواضع المطابقة في
  كل كتاب على حدة، فتجيب عن السؤال الذي لم يكن للأداة جواب عنه: هل شمل البحث جميع
  الكتب؟ تعرض كم كتابًا بُحث فيه، وكم كتابًا فيه مطابقة فعلًا، وتوزيع المواضع على الكتب
  مرتَّبة بالأكثر. تعمل على المكتبة كلها (٨٥٩٨ كتابًا) في أقل من ثانية، لأنها تقرأ أعدادًا
  لا صفحات. وكان محرّك العدّ هذا موجودًا في `java/src` و`bridge.py` منذ الإصدار الأول
  ولم يكن معروضًا كأداة، فلم يكن أحد يبلغه.
- **سطر البصمة في `shamela_health` يظهر دائمًا** لا عند القِدَم فقط، ليتحقّق المستخدم بعد
  أي تحديث من أن كلود يشغّل النسخة الجديدة فعلًا.
- **حزمة `.mcpb`: تثبيت بنقرتين.** طريق ثانٍ للتثبيت: يُنزَّل ملف واحد ويُفتح بكلود، بلا
  فكّ ضغط ولا نافذة أوامر ولا تثبيت بايثون. وسببُ وجوده أن **Smart App Control** في
  ويندوز ١١ يمنع `setup.bat` النازل من الإنترنت منعًا لا مخرج منه — لا زرّ «Run anyway»
  فيه — بينما الحزمة بياناتٌ يفتحها التطبيق لا سكربتٌ يشغّله ويندوز. تُبنى بـ
  `scripts/build_mcpb.py`، ومصدرها في `mcpb/`، وتعلن نوع الخادم `uv` فيتولّى المضيف
  بايثون والمتطلّبات. والمثبّت باقٍ لمن أعوزه تشخيص أو تثبيت بايثون تلقائي.

### تغيّر (Changed)

- **الدفعة الواحدة لم تعد لكتاب واحد.** كان ترتيب النتائج بالتشابه اللفظي وحده، فيبتلع
  كتابٌ ضخمٌ متكرّر اللفظ الدفعة كلها، فيظنّ القارئ أن المسألة نادرة وفي المكتبة عشرات
  الكتب فيها. صار لا يظهر من الكتاب الواحد في الدفعة إلا موضعان، مع بيان عدد المواضع
  التي أُخِّرت لذلك. والقيد يسقط من نفسه حين لا يكون في النتائج تنوّع يكفي (كأن تكون
  المطابقات كلها في كتاب أو كتابين)، فلا يضيع بسببه شيء.
- **ترقيم النتائج صار متّصلًا بين الدفعات** (٦–١٠ في الدفعة الثانية بدل ١–٥ مرة أخرى)،
  ومعه تنبيه صريح حين يكون المعروض أقلّ من إجمالي المطابقات — فترك `cursor` بعد دفعة
  واحدة كان يُقرأ كأن الباب استُوفي.
- **`update.bat`: طريقة معلنة للتحديث.** تجلب آخر شيفرة (إن كان المجلد نسخة `git`)،
  وتحدّث المتطلّبات، وتتحقّق من سلامة التسجيل في كلود، وتجرّب بحثًا حقيقيًّا — دون المساس
  بالإعدادات ولا بالمكتبة، ودون إعادة تشغيل `setup.bat`. وخطوات المثبّت صارت تُرقَّم
  ترقيمًا تلقائيًّا ليخدم المسارين.

</div>

## [غير منشور] (Unreleased)

<div dir="rtl">

### أُصلح (Fixed)

- **مسار ملف المحرّك (`jar`) كان يفترض شكلًا واحدًا للتثبيت.** كان يُحسب من موضع الحزمة
  على أنه `‏../java` — وهو صحيح في نسخة المستودع وحدها. فأيّ تثبيت آخر (حزمة `.mcpb`،
  أو تثبيت `pip` غير `editable`) يشير إلى مسار لا وجود له، ولا يظهر العطل إلا عند أول
  بحث. صار يُبحث عنه في المواضع المحتملة كلها، ويمكن تثبيته بمتغيّر `SHAMELA_MCP_JAR`.

- **دليل التثبيت كان يحيل على صفحة إصدارات فارغة**، ولا يذكر ما يعترض المستخدم فعلًا:
  وسم ويندوز للملفات النازلة من الإنترنت، والمجلد المتداخل الذي يخفي `setup.bat`،
  ومنع Smart App Control الذي لا مخرج منه في نافذته. صار التنزيل من رابط عامل، ومعه
  علاج مرتَّب لكل واحدة من هذه.

- **نصّ الصفحة لم يكن يصل إلى كلود أصلًا.** كانت الأداة تُرسل قناتين: نصّ الصفحة كاملًا في
  قناة النصّ (`content`)، وبيانات مجرّدة بلا نصّ في `structuredContent` (تفاديًا لدفع كلفة
  النص مرتين). لكنّ Claude Desktop يقرأ `structuredContent` حين يوجد، فكان يصل العزو
  الكامل (الكتاب/الجزء/الصفحة/الباب) مع `text_length` غير صفري، ولا يصل النص. فيضطر
  النموذج إلى التلخيص من حفظه بدل النقل الحرفي — وهو عين ما وُجد هذا الخادم ليمنعه.
  أصبح `text_original` والحاشية يُرسلان في القناتين معًا. يشمل الإصلاح `shamela_search`
  و`shamela_search_category` و`shamela_search_book` و`shamela_get_page`.


- **نافذة `setup.bat` كانت تُغلق نفسها بلا رسالة** بعد أول ضغطة مفتاح، فلا يعرف المستخدم
  هل تمّ التثبيت أم لا. وكان السبب في ملف الباتش نفسه: نهايات أسطر LF بدل CRLF، ونصوص
  عربية (UTF-8) داخله — و`cmd.exe` يقرأ ملفات الباتش بترميز الطرفية فيتلف تحليلها في
  الحالتين، فتفشل القفزات (`goto`) ويخرج البرنامج صامتًا. أُعيدت كتابة `setup.bat`
  و`uninstall.bat` بالإنجليزية الصافية (ASCII) ونهايات CRLF، ونُقلت الرسائل العربية إلى
  `install.py`، وما يلزم منها قبل توفّر بايثون إلى ملفات نصية UTF-8 تُعرض بأمر `type`.
- **تصحيح استدعاء مشغّل بايثون**: كان `"py -3"` يُحاط بعلامتَي تنصيص كأنه اسم ملف واحد،
  فيرفضه ويندوز. فُصل المشغّل عن معطاه.
- **ضمان أن النافذة لا تُغلق أبدًا بلا بيان**: كل مسارات الخروج تنتهي بانتظار ضغطة مفتاح،
  ويُفتح `install.log` تلقائيًّا عند أي إخفاق.

- **كلود كان يشغّل نسخة منقولة قديمة من الشيفرة.** كان المثبّت ينسخ الحزمة إلى البيئة
  المعزولة نسخًا (`pip install .`)، وكلود يشغّل الخادم من خارج مجلد المشروع، فيستورد تلك
  النسخة لا الملفات الأصلية. فكان تعديل الشيفرة ثم إعادة تشغيل كلود لا يُغيّر شيئًا، ويبدو
  الإصلاح وكأنه فشل. صار التثبيت تحريريًّا (`pip install --editable`)، فالملفات التي في
  مجلد المشروع هي التي تعمل، ويكفي بعد أي تحديث إغلاق كلود وفتحه دون إعادة تثبيت.

- **كشف النسخة القديمة**: يُبلّغ `shamela_health` الآن ببصمة الشيفرة العاملة ويقارنها
  بالملفات على القرص. فإن كان Claude Desktop يشغّل نسخة قديمة (وهو يُبقي الخادم عاملًا
  بين المحادثات) أعلن ذلك صراحةً وطلب إغلاق التطبيق إغلاقًا تامًّا — بدل أن يبدو الإصلاح
  وكأنه لم ينجح.

### أُضيف (Added)

- اختبارات تحرس ملفات الباتش (`tests/test_batch_files.py`): تتحقّق من خلوّها من أي بايت
  غير ASCII، ومن نهايات CRLF، ومن أن كل `goto` له هدف موجود، ومن أن كل مسار خروج ينتظر
  ضغطة مفتاح. وقد تحُقّق من أنها تكشف الخطأ الأصلي فعلًا.

</div>

## [1.0.0] — 2026-08-29

<div dir="rtl">

الإصدار الأول.

### أُضيف (Added)

- **بحث في نصوص المكتبة الشاملة** بثلاثة نطاقات: المكتبة كلها، أو أقسام مختارة،
  أو كتاب واحد بعينه.
- **توجيه تلقائي حسب الفنّ**: أرقام أقسام المكتبة وأسماؤها تُقرأ من مكتبة المستخدم
  نفسها عند التشغيل وتُدرج في وصف الأداة، فيختار كلود القسم المناسب للسؤال وحده
  (أحكام العبادات ← أقسام الفقه، تفسير آية ← قسم التفسير، تخريج حديث ← كتب السنة).
- **ثلاثة أنماط للمطابقة**: عبارة متتابعة، أو كل الكلمات، أو أيّ كلمة. والاستعلام
  الموضوع بين «» يُعدّ عبارة متتابعة تلقائيًّا.
- **بحث بالجذر** يشمل مشتقات الكلمة (الطلاق ← يطلق، مطلقة، طالق)، مبنيّ على قاعدة
  الجذور المرفقة بالشاملة نفسها، مع رجوع تلقائي إلى البحث الحرفي والتنبيه عليه إن لم
  يكن للكلمة جذر مسجَّل.
- **نصّ الصفحة كاملًا** مع كل موضع، لا مقتطفات — ليتّضح السياق قبل النقل.
- **إحالة جاهزة** مع كل موضع: `[الكتاب | الجزء | الصفحة | الباب | المؤلف (ت …)]`،
  وسلسلة الأبواب كاملة من الأعلى إلى الأدنى (كتاب الصلاة › باب صلاة الجماعة).
- **عرض الصفحات بسياقها**: صفحة بمعرّفها أو بترقيمها المطبوع، ومعها ما قبلها وما بعدها.
- **فهرس الكتب** بشجرة أبوابه ومستوياته، وبطاقة كل كتاب، والبحث عن الكتب بأسماء
  مؤلفيها.
- **أداة فحص** تعرض حالة المكتبة والفهرس وجافا وقاعدة الجذور، وتتحقّق من مطابقة قواعد
  التطبيع للفهرس فعليًّا — استدعاؤها بعبارة «افحص مكتبة الشاملة».
- **دليل عربي** داخل الأداة لمنهج العمل والأمثلة والحدود.
- **مثبّت بنقرة مزدوجة** (`setup.bat`) يجد المكتبة، ويهيّئ ما يلزم، ويجرّب بحثًا
  حقيقيًّا، ثم يضيف الخادم إلى إعدادات كلود مع نسخة احتياطية — ومزيل مقابل
  (`uninstall.bat`).
- **رسائل الأخطاء بالعربية** مع بيان ما ينبغي عمله، وتمييز «لم تُوجد مطابقة» من
  «لم يُبحث أصلًا».

### ملاحظات تقنية (Technical notes)

- يعمل الخادم محليًّا بلا اتصال بالشبكة، ويقرأ ملفات الشاملة قراءةً فقط.
- يستعمل جافا ومحرك البحث Lucene المرفقين أصلًا مع المكتبة الشاملة، فلا يحتاج
  المستخدم تثبيت جافا، ولا يشحن المشروع أيّ ملف من ملفات الشاملة.
- تصحيح مهم في التطبيع: الهمزة على نبرة تُطبَّع ياءً (`ئ ← ي`) كما يفعل فهرس الشاملة؛
  وإغفال ذلك يجعل كل بحث يحتوي هذا الحرف يرجع بلا نتائج بلا سبب ظاهر.
- مؤشّرات التصفّح مرتبطة بحالة الفهرس، فإن أعادت الشاملة بناء فهارسها بطل المؤشّر
  وأُعلن ذلك، ولم تُستأنف النتائج من موضع تغيّر معناه.

</div>

[غير منشور]: https://github.com/mMubark/shamela-mcp/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/mMubark/shamela-mcp/releases/tag/v1.0.0
