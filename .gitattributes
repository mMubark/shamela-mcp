# Auto detect text files and perform LF normalization
* text=auto

# Windows batch files must keep CRLF: some cmd.exe versions mis-parse LF-only .bat
*.bat text eol=crlf

# The shipped helper is binary
*.jar binary
